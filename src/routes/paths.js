// Centraliza os caminhos das rotas para evitar strings repetidas no código.
const ROUTES = {
  home: '/',
  uiCatalog: '/componentes',
  login: '/login',
  register: '/cadastro',
  feed: '/feed',
};

export { ROUTES };
